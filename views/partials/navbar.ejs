<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand text-uppercase" href="/">
      <img
        src="/img/logo2.svg"
        alt="Astronaut and spaceship"
        width="30"
        height="24"
        class="d-inline-block align-text-top"
      />
      Astronauts</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
        <% if(!currentUser) { %>
        <li class="nav-item"><a class="nav-link <%= path === "/users/login" ? "active" : "" %>" href="/login">Sign in</a></li>
        <li class="nav-item ms-lg-3">
          <a class="text-uppercase btn btn-light" href="/register">Sign up</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link <%= path === "/astronauts/all" ? "active" : "" %>" href="/astronauts/all">Your astronauts</a>
        </li>
        <li class="nav-item ms-lg-3">
          <a class="nav-link <%= path === "/astronauts/add" ? "active" : "" %>" href="/astronauts/add">Add new astronaut</a>
        </li>
        <li class="nav-item ms-lg-3">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
